/*$('#setting_btn_save').on('click', function(){
    layer.msg('保存成功!');
    /!*var index = parent.layer.getFrameIndex(window.name); //获取窗口索引
    parent.layer.close(index);*!/
});



$('#setting_btn_close').on('click', function(){
    var index = parent.layer.getFrameIndex(window.name); //获取窗口索引
    parent.layer.close(index);
});*/

